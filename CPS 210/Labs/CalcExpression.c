/*
 * Homework2-CalcExpression.c
 * CPS 210
 * Chris Phillips
 * 2/1/17
 * Homework 2: Calc Expressions
 *
 * Description:
 * This program allows the user to enter 4 numbers
 * and receive the answer to the equation provided.
 */

#include <stdio.h>
#include <math.h>

//int main(void) {
//
//	int a, b, c, d;
//	double numerator, denominator, extra, final;
//
//	printf("Enter a: " );
//	scanf("%d", &a);
//
//	printf("Enter b: " );
//	scanf("%d", &b);
//
//	printf("Enter c: " );
//	scanf("%d", &c);
//
//	printf("Enter d: " );
//	scanf("%d", &d);
//
//	numerator = sqrt(pow(b, 10) - (4*a*c) + (double)(a)/c); //Calculates the top part of the equation
//	denominator = log10(d) / log10(b); //Calculates the bottom part of the equation
//	extra = fmod(pow(a, c), b); //Calculates the end part of the equation
//	final = (numerator/denominator) + extra; //Combines all the parts of the equation
//
//	//Prints out the results
//	printf("Your input of %d, %d, %d, and %d yields: \n\n", a, b, c, d);
//	printf("%s%.4lf%s\n%20s\n%s%.0lf%s%.4lf", "The number ", final, " with four decimal places", "OR", "The number ", final, " rounded up from ", final);
//
//}

